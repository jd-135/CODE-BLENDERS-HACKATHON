# BIZ HACK'26 Environment Configuration
# Switch between 'local' (offline zero-dependency store) and 'supabase'
NEXT_PUBLIC_DATA_SOURCE=local

# Optional Supabase credentials (populated if cloud sync is activated)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
